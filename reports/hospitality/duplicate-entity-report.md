# Duplicate entity report

- Duplicate `entity_id` values: 0
- Duplicate appended records: 0
- Shared slugs are retained where the source intentionally represents different semantic record types.
- Current map entities are updated by ID, then slug fallback; they are never appended as new records.
