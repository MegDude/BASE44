#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

branch="$(git rev-parse --abbrev-ref HEAD)"
echo "Running preview validation for $branch"

npm run preview:validate
