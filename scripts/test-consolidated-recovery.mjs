import assert from"node:assert/strict";import{readFileSync}from"node:fs";const f=p=>readFileSync(p,"utf8"),m=f("src/pages/Map.jsx"),r=f("src/lib/map/mapIconRegistry.ts"),a=f("src/lib/AuthContext.jsx"),x=f("src/pages/partners/Access.jsx"),c=f("src/pages/AuthCallbackPage.jsx"),p=f("src/lib/authReturnPath.ts");assert.match(m,/resolveCanonicalLegendsDirectoryPlaces/);assert.doesNotMatch(m,/legendsDirectoryPlaces\.length \|\| discoverDisplayPlaces\.length/);assert.match(r,/legends-logo-gold\.svg/);assert.match(a,/shouldCreateUser:false|shouldCreateUser: false/);assert.match(a,/type: "confirmation_sent"/);assert.match(x,/audience=partner&returnTo/);assert.match(x,/secure sign-in link could not be completed/);assert.match(c,/audience === "partner"/);assert.match(p,/normalizeAuthReturnPath/);console.log("PASS");
